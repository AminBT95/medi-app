# Correction navigation Rôles → Pro

- Le bouton **Rôles** de l’interface patient ouvre désormais `/pro`.
- `/roles` est conservé comme alias et redirige automatiquement vers `/pro`.
- Le correctif est appliqué à `client/simple.html` et à la copie utilisée par le build mobile.
- Le tableau de bord `/pro` original Replit reste inchangé.
