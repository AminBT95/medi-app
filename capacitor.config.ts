import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elabt2095.medirappel',
  appName: 'Medi-Rappel',
  webDir: 'dist/public',
  server: { androidScheme: 'https' },
};
export default config;
