# Browser dashboard can leave this empty when served by the same Express server.
# The Android build MUST point to your deployed HTTPS API.
VITE_API_URL=https://api.example.com
DATABASE_URL=postgresql://user:password@host:5432/medirappel
CORS_ORIGINS=https://app.example.com,capacitor://localhost,http://localhost
PORT=5000
SEED_DATABASE=false
JWT_SECRET=change-me-with-a-long-random-secret
